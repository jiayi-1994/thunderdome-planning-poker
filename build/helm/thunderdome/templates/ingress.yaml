{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "thunderdome.fullname" . }}
  labels:
    {{- include "thunderdome.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.app.domain | quote }}
      secretName: {{ required "ingress.tls.secretName is required when TLS is enabled" .Values.ingress.tls.secretName | quote }}
  {{- end }}
  rules:
    - host: {{ .Values.app.domain | quote }}
      http:
        paths:
          - path: {{ default "/" .Values.app.pathPrefix | quote }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "thunderdome.fullname" . }}
                port:
                  name: http
{{- end }}
